# Copyright 2026 Adam Joseph Rivers, CEO, Synthicsoft Labs LLC.
# Licensed under the Apache License, Version 2.0.
name: Nessy Autonomous Control Plane

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened]
  issues:
    types: [opened]
  workflow_run:
    workflows:
      - CI
      - Preflight
      - Integrity
      - Interposition Integrity
      - Nessy Chat Artifact
      - CodeQL
    types: [completed]
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write
  checks: read
  actions: read
  copilot-requests: write

env:
  GH_TOKEN: ${{ github.token }}
  GH_REPO: ${{ github.repository }}

concurrency:
  group: nessy-autonomous-control-${{ github.event.pull_request.number || github.event.issue.number || github.event.workflow_run.id || github.ref_name }}
  cancel-in-progress: true

jobs:
  dispatch:
    name: Autonomous control and recovery
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      target_ref: ${{ steps.context.outputs.target_ref }}
      issue_number: ${{ steps.context.outputs.issue_number }}
      objective: ${{ steps.context.outputs.objective }}
    steps:
      - name: Resolve execution context
        id: context
        shell: bash
        run: |
          set -euo pipefail
          event='${{ github.event_name }}'
          target_ref='main'
          issue_number=''
          objective=''
          case "$event" in
            pull_request)
              target_ref='${{ github.event.pull_request.head.ref }}'
              issue_number='${{ github.event.pull_request.number }}'
              objective="Validate and complete pull request #${{ github.event.pull_request.number }}: ${{ github.event.pull_request.title }}" ;;
            issues)
              target_ref='main'
              issue_number='${{ github.event.issue.number }}'
              objective='${{ github.event.issue.title }}: ${{ github.event.issue.body }}' ;;
            workflow_run)
              target_ref='${{ github.event.workflow_run.head_branch }}'
              objective="Repair or reconcile workflow run ${{ github.event.workflow_run.id }} (${{ github.event.workflow_run.name }}, ${{ github.event.workflow_run.conclusion }}, SHA ${{ github.event.workflow_run.head_sha }}). Inspect the run, logs, checks, affected code, and repository state, then complete the engineering cycle." ;;
            push)
              target_ref='main'
              objective='Run the complete autonomous repository development cycle against the new main commit. Inspect lineage, code, tests, architecture, documentation, automation, runtime/chat surfaces, model/provider catalog, capability graph, security, integrity, licensing, release surfaces, and active workflows. Repair defects and implement the highest-value missing capability discovered from repository evidence.' ;;
            schedule)
              target_ref='main'
              objective='Run the complete autonomous Nessy self-development cycle. Discover the highest-value unresolved engineering opportunity across functionality, tests, security, redundancy, recovery, capability coverage, model/provider integration, chat UX, infrastructure, performance, documentation, research ingestion, release engineering, and maintainability. Use docs/SELF_DEVELOPMENT.md to select one atomic objective and execute the complete engineering cycle.' ;;
            workflow_dispatch)
              target_ref='main'
              objective='Run the complete autonomous repository development and health cycle from current main state.' ;;
          esac
          echo "target_ref=$target_ref" >> "$GITHUB_OUTPUT"
          echo "issue_number=$issue_number" >> "$GITHUB_OUTPUT"
          {
            echo 'objective<<NESSY_OBJECTIVE'
            printf '%s\n' "$objective"
            echo 'NESSY_OBJECTIVE'
          } >> "$GITHUB_OUTPUT"

      - name: Record controller receipt
        shell: bash
        env:
          ISSUE_NUMBER: ${{ steps.context.outputs.issue_number }}
          OBJECTIVE: ${{ steps.context.outputs.objective }}
        run: |
          set -euo pipefail
          if [ -n "$ISSUE_NUMBER" ]; then
            gh issue comment "$ISSUE_NUMBER" --repo "$GH_REPO" --body "Nessy Autonomous Control Plane accepted this machine event and is invoking the autonomous engineering worker as a native reusable workflow."
          fi
          echo '### Nessy Autonomous Control Plane' >> "$GITHUB_STEP_SUMMARY"
          echo "- Event: ${{ github.event_name }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Target ref: ${{ steps.context.outputs.target_ref }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Objective: $OBJECTIVE" >> "$GITHUB_STEP_SUMMARY"

  engineer:
    name: Autonomous engineering worker
    needs: dispatch
    uses: ./.github/workflows/nessy-autonomous-engineer.yml
    with:
      target_ref: ${{ needs.dispatch.outputs.target_ref }}
      issue_number: ${{ needs.dispatch.outputs.issue_number }}
      objective: ${{ needs.dispatch.outputs.objective }}
    secrets: inherit
